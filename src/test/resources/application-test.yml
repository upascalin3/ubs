spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wasac_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: false

jwt:
  secret: test-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm

app:
  file:
    upload-dir: ./target/test-uploads
